## Summary

- Describe what changed and why.

## Checklist

- [ ] Updated `CHANGELOG.md` (`[Unreleased]`) with user-facing changes.
- [ ] Version sync checked (`package.json` == `skills/leak/SKILL.md`) when release-related files changed.
- [ ] Skill docs/scripts updated when `/skills/leak` behavior changed.
- [ ] Verified core commands still work (`leak --help`, `leak config --help`, buy/publish path touched by this PR).
- [ ] Added/updated tests or smoke checks where practical.

## Rollout / Risk

- Risk level: low / medium / high
- Rollback plan:
